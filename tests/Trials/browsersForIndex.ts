const otherBrowsers = ["Chrome", "Firefox", "WebKit"];

for (let i = 0; i < otherBrowsers.length; i++) {
    if (i === otherBrowsers.length - 1) {
        console.log(`Last browser is ${otherBrowsers[i]}`);
    }
}